import 'package:flutter/material.dart';

class DireccionPage extends StatelessWidget {
  const DireccionPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Direccion page')),
    );
  }
}
